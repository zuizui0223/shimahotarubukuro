# Raw specimen scans and image data (kept out of the repo)
*.jpg
*.jpeg
*.JPG
*.JPEG
data/
shimahotarubukuro/

# QC overlays embed the specimen scans — regenerate locally
results/overlays/

# Shimask-input all-sheet run: overlay/mask PNGs embed the specimen scans —
# keep the measured CSV/text outputs, regenerate the imagery locally
results_shimask_all/**/*.png
# Plotting cache of pooled guide pixels (regenerated by guide_spatial.py)
results_shimask_all/*.npz

# Review-app runtime state & caches (regenerated locally)
results/review_state/
results/review_cache/
results/reviewed/*/app_review/
results_single/
results_all_review/

# Python
__pycache__/
*.pyc
.venv/
venv/
mask/

# Core pipeline + review app. Keep native image packages pinned: broad >= ranges
# pulled NumPy 2.5 / OpenCV 5 immediately after their July 2026 releases and the
# Streamlit worker terminated inside native code with SIGSEGV.
numpy==2.4.2
opencv-python-headless==4.13.0.92
pillow>=10.0
openpyxl>=3.1
streamlit==1.57.0
streamlit-image-coordinates>=0.1.9

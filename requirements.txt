# Core pipeline + review app. opencv-python-headless (not opencv-python) so it
# imports on servers/Streamlit Cloud without libGL. Streamlit Cloud installs from
# this file, so the app deps live here too.
numpy>=1.24
opencv-python-headless>=4.8
pillow>=10.0
openpyxl>=3.1
streamlit==1.57.0
streamlit-image-coordinates>=0.1.9
streamlit-drawable-canvas>=0.9.3
